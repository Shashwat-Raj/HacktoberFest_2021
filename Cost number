#include<bits/stdc++.h>
using namespace std;


int main()
{
	int n;
	cin>>n;
	if(n>0)
	{
		
	if(n>=100 && n%10==0)
		cout<<-6<<endl;
	else if(n>=100)
		cout<<-4<<endl;
	else if(n%10==0)
		cout<<-5<<endl;
	else 
	cout<<n<<endl;
	}
}
